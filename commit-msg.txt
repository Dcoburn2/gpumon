Take the last three Linux failures out

test_modes: the icon check was relabelled but not changed - my earlier replacement
looked for "gpumon.ico" while the text says ".ico" - so it still demanded an icon
Windows uses. It now asks for the one the platform uses.

test_cputemp: a second check, further down, still looked only for the Windows
wording of the missing-temperature note.

test_acceptance: loaded the machine with a PowerShell busy loop, passing
creationflags - Windows-only twice over. A Python loop says the same thing about
the interface staying responsive and works on either platform, with the keyword
left out where subprocess refuses it.

And in the program, one more silent backend: AcpiThermalZoneBackend set its reason
inside available() only, so calling poll() directly - which is what the suite does -
returned an empty dict with no explanation. An unavailable backend that says
nothing cannot be told apart from one that looked and found nothing.

Windows suite green: 39 files, 0 failures.
